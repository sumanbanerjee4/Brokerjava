package com.home.service;

import org.springframework.beans.factory.annotation.Autowired;

import com.home.model.SignUp;
import com.home.repo.SignUpRepository;

public class SignUpService {
	
	/*@Autowired
	SignUpRepository signupRepository;

	public SignUp save(SignUp user) {
		return signupRepository.saveAndFlush(user);
	}

	public SignUp update(update user) {
		return signupRepository.save(user);
	}

	public SignUp find(String userName) {
		return signupRepository.findOneByUsername(userName);
	}

	public SignUp find(Long id) {
		return signupRepository.findOne(id);
	}
*/
}
